#context specific configuration file
#if property starts with validation, then it goes to validation context
#if property starts with normalizer, then it goes to normalization context
#if property starts with global, then it goes to global context
# so on ..... etc.
normalizer.ndl.subject.ddc=org.iitkgp.ndl.context.custom.normalizer.NDLCareerVerticalDDCNormalizer